# ConvoDesk CRM – API Contract

This file will define the FastAPI routes, request bodies, response shapes, and page-to-endpoint mapping.

Planned route groups:

* /auth
* /contacts
* /conversations
* /messages
* /followups
* /analytics
* /ai

Detailed API contract to be finalized after database schema is locked.
